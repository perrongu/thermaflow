{
  "name": "thermaflow",
  "version": "1.1.7",
  "private": true,
  "description": "Analyse du risque de gel dans les conduites d'eau",
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.1.0",
    "eslint-config-prettier": "^9.1.0"
  },
  "scripts": {
    "lint": "eslint js/ tests/ scripts/ --ext .js",
    "lint:fix": "eslint js/ tests/ scripts/ --ext .js --fix",
    "format": "prettier --write \"**/*.{js,css,html,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,md}\"",
    "verify": "node tests/automated_verification.js"
  }
}
